export declare enum Keyword {
    IF = "if",
    ELSE = "else",
    DO = "do",
    WHILE = "while",
    CONTINUE = "continue",
    BREAK = "break",
    SWITCH = "switch",
    CASE = "case",
    DEFAULT = "default"
}
export declare enum Punctuation {
    BRACE_L = "{",
    BRACE_R = "}",
    PAREN_L = "(",
    PAREN_R = ")",
    COLON = ":",
    SEMICOLON = ";"
}
export declare enum TokenType {
    PUNCTUATION = "punctuation",
    NODE = "node",
    KEYWORD = "keyword"
}

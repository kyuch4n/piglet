export enum Keyword {
  IF = "if",
  ELSE = "else",
  DO = "do",
  WHILE = "while",
  CONTINUE = "continue",
  BREAK = "break",
  SWITCH = "switch",
  CASE = "case",
  DEFAULT = "default"
}

export enum Punctuation {
  // BRACKET_L = "[",
  // BRACKET_R = "]",
  BRACE_L = "{",
  BRACE_R = "}",
  PAREN_L = "(",
  PAREN_R = ")",
  COLON = ":",
  SEMICOLON = ";"
}
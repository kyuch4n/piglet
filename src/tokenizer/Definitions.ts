export enum Keyword {
  RETURN = "return",
  IF = "if",
  ELSE = "else",
  DO = "do",
  WHILE = "while"
}

export enum Punctuation {
  BRACKET_L = "[",
  BRACKET_R = "]",
  BRACE_L = "{",
  BRACE_R = "}",
  PAREN_L = "(",
  PAREN_R = ")",
  SEMICOLON = ";"
}